using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test_event_data : MonoBehaviour
{

    public string title;
    public string body;

    // Start is called before the first frame update
    void Start()
    {
        title = "event 1";
        body = "dit is event 1...";
    }
}
